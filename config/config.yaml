# ============================================================
# Predictive & Preference-Aware Compiler Optimization
# Global Configuration
# ============================================================

project:
  name: "Predictive-Preference-Aware-Compiler-Optimization"
  version: "1.0.0"
  author: "Satyam Singh Kumre"
  institution: "NIT Tiruchirappalli"

paths:
  data_dir: "data"
  raw_dir: "data/raw"
  formai_dir: "data/raw/formai"
  kaggle_dir: "data/raw/kaggle_c_programs"
  features_dir: "data/features"
  labels_dir: "data/labels"
  processed_dir: "data/processed"
  results_dir: "result_real"
  figures_dir: "result_real/figures"
  tables_dir: "result_real/tables"
  logs_dir: "result_real/logs"
  models_dir: "result_real/models"

features:
  autophase_dim: 56
  graph_features_dim: 32
  priority_vector_dim: 4
  total_feature_dim: 95  # 56 + 32 + 3 (language) + 4

dataset:
  num_benchmarks: 30
  num_pass_sequences: 50
  train_ratio: 0.7
  val_ratio: 0.15
  test_ratio: 0.15
  random_seed: 42
  num_optimization_classes: 5

models:
  random_forest:
    n_estimators: 1000
    max_depth: 20
    min_samples_split: 5
    min_samples_leaf: 2
    max_features: "sqrt"
    random_state: 42
    n_jobs: -1
    cv_folds: 5

  xgboost:
    n_estimators: 1000
    max_depth: 8
    learning_rate: 0.05
    subsample: 0.8
    colsample_bytree: 0.8
    reg_alpha: 0.1
    reg_lambda: 1.0
    random_state: 42
    early_stopping_rounds: 30
    eval_metric: "mlogloss"

  dnn:
    hidden_layers: [256, 128, 64]
    dropout_rate: 0.3
    batch_norm: true
    learning_rate: 0.001
    batch_size: 32
    max_epochs: 500
    patience: 50
    weight_decay: 0.0001
    activation: "relu"
    optimizer: "adam"

  ensemble:
    method: "weighted_voting"
    weight_optimization: "validation_f1"

evaluation:
  metrics:
    - "execution_time_speedup"
    - "code_size_reduction"
    - "f1_score"
    - "compilation_overhead"
    - "adaptability_score"
  significance_level: 0.05
  num_bootstrap_samples: 1000

priority_presets:
  speed_first: [0.7, 0.2, 0.1, 0.0]
  size_first: [0.2, 0.7, 0.1, 0.0]
  balanced: [0.25, 0.25, 0.25, 0.25]
  energy_efficient: [0.3, 0.3, 0.3, 0.1]
  compile_fast: [0.1, 0.1, 0.7, 0.1]
  secure_enclave: [0.10, 0.10, 0.05, 0.75]
  crypto_fast: [0.40, 0.10, 0.10, 0.40]

llvm_passes:
  # 50 common LLVM optimization passes
  available_passes:
    - "-mem2reg"
    - "-simplifycfg"
    - "-instcombine"
    - "-reassociate"
    - "-gvn"
    - "-licm"
    - "-loop-unroll"
    - "-loop-rotate"
    - "-loop-simplify"
    - "-indvars"
    - "-sccp"
    - "-dce"
    - "-adce"
    - "-deadargelim"
    - "-inline"
    - "-sroa"
    - "-early-cse"
    - "-jump-threading"
    - "-correlated-propagation"
    - "-tailcallelim"
    - "-loop-idiom"
    - "-loop-deletion"
    - "-loop-vectorize"
    - "-slp-vectorizer"
    - "-memcpyopt"
    - "-dse"
    - "-bdce"
    - "-constmerge"
    - "-globaldce"
    - "-globalopt"
    - "-ipsccp"
    - "-called-value-propagation"
    - "-float2int"
    - "-lower-constant-intrinsics"
    - "-loop-distribute"
    - "-loop-load-elim"
    - "-alignment-from-assumptions"
    - "-strip-dead-prototypes"
    - "-div-rem-pairs"
    - "-mergefunc"
